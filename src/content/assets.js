

const assetsPath = '';
const iconsPath = `${assetsPath}/icons/`;
export default assets = {
    play: require('../../assets/icons/play.png'),
    pause: `${iconsPath}/pause.svg`,
    skip: `${iconsPath}/skip.svg`,
}